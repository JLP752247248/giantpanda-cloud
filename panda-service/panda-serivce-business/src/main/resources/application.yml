spring:
  application:
    name: business
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        ###服务注册地址
        server-addr: localhost:8848
      config:
        file-extension: yaml #文件后缀名

server:
  port: 8081